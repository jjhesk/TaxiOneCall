<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--this is the dialog styling configuration -->
    <style name="MaterialDialogSheet" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/filter_background</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowAnimationStyle">@style/MaterialDialogSheetAnimation</item>
    </style>

    <!-- material dialog stylings-->
    <style name="MaterialDialogSheetAnimation">
        <item name="android:windowEnterAnimation">@anim/popup_show</item>
        <item name="android:windowExitAnimation">@anim/popup_hide</item>
    </style>

    <!-- material dialog stylings-->
    <style name="materialStyleSearchView">
        <!-- Background for the search bar-->
        <item name="searchBackground">@android:color/white</item>
        <item name="searchOverlayColor">@color/overlay_network</item>
        <!-- Change voice icon-->
        <item name="searchVoiceIcon">@drawable/microphones</item>
        <!-- Change clear text icon-->
        <item name="searchCloseIcon">@drawable/ic_action_navigation_close_inverted</item>
        <!-- Change up icon-->
        <item name="searchBackIcon">@drawable/ic_close_24dp</item>
        <!-- Change background for the suggestions list view-->
        <item name="searchSuggestionBackground">@color/white_screen</item>
        <!-- Hint for edit text-->
        <item name="android:hint">@string/search_hint</item>
    </style>

</resources>